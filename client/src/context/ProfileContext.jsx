/**
 * ProfileContext.jsx
 * Context that stores the user's data and tokens
 */

import { createContext } from "react"; // Importing hooks from react

const ProfileContext = createContext(); // Creating a context to hold the user profile

export default ProfileContext;
